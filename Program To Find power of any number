#include<iostream>
using namespace std;
long long int powerr(long long int x,long long int y){
    if(y==0)
    return 1;
    else
    return x*powerr(x,y-1);   
}
int main(){
    long long int base,power;
    cin>>base;
    cin>>power;
    cout<<powerr(base,power)<<endl;
}
