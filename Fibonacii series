#include<stdio.h>
int fib(int m){
if(m==0||m==1)
  return m;
else
  return (fib(m-1)+fib(m-2));
}

int main()
{
  int n;
  printf("Enter the number");
  scanf("%d",&n);
  int res=fib(n);
  printf("%d",res);
}
