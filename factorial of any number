#include <iostream> 
using namespace std; 

long facto(long n) 
{ 
    if (n == 0) 
        return 1; 
    return n * facto(n - 1); 
} 

int main() 
{ 
    long num ;
    cin>>num; 
    cout << "Factorial of "<< num << " is = " << facto(num) << endl; 
    return 0; 
} 
