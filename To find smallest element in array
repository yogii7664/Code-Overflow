#include <stdio.h>

int main() {
int i ,min,a[5];
printf("\n enter 5 elements \n");
for(i=0;i<5;i++)
{
    scanf("%d",&a[i]);
}
min=a[0];
for(i=0;i<5;i++)
{
    if(min>a[i])
    {
        min=a[i];
    }
}
printf("smallest element is %d ",min);
}
