var numbers = [10,56,16,1,90,25,-7,58,62,111];
console.log(numbers);
var highest = Number.MIN_VALUE;

for(var i = 0; i < numbers.length; i++)
{
    if(numbers[i] > highest)
    {
        highest = numbers[i];
    }
}

console.log(highest);
