#include <iostream>
#include <math.h>
using namespace std;
 
int main()
{
  long bi, deci = 0, j = 1, r;
	cout << " Input a binary number: ";
	cin>> bi;
  while (bi != 0) 
  {
   r = bi%10;
   deci = deci + r*j;
   j = j*2;
   bi = bi/10;
  }
  cout<<" The decimal number: " << deci<<"\n";
} 
